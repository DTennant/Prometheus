[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-lib"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["agent_lib"]
